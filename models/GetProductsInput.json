{
  "type": "object",
  "properties": {
    "pageFrom": {
      "type": "number",
      "minimum": 0
    },
    "pageSize": {
      "type": "number",
      "minimum": 0
    },
    "searchText": {
      "type": "string"
    },
    "sorts": {
      "type": "object",
      "properties": {
        "family": {
          "type": "string",
          "enum": ["asc", "desc"]
        },
        "enabled": {
          "type": "string",
          "enum": ["asc", "desc"]
        },
        "id": {
          "type": "string",
          "enum": ["asc", "desc"]
        },
        "createdAt": {
          "type": "string",
          "enum": ["asc", "desc"]
        },
        "updatedAt": {
          "type": "string",
          "enum": ["asc", "desc"]
        },
        "label": {
          "type": "string",
          "enum": ["asc", "desc"]
        }
      }
    },
    "filters": {
      "type": "object",
      "properties": {
        "family": {
          "type": "string"
        },
        "parent": {
          "type": "string"
        },
        "variant": {
          "type": "string"
        },
        "dataType": {
          "type": "string",
          "enum": [
            "PRODUCT",
            "PRODUCT_MODEL"
          ]
        },
        "isVariant": {
          "type": "boolean"
        },
        "group": {
          "type": "string"
        },
        "forceGroupFilter": {
          "type": "boolean"
        }
      }
    }
  }
}
